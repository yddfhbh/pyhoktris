# kannyan-tetris

Tetris client/server scaffold.

## Structure

- `client/`: Vite-based frontend scaffold
- `server/`: Node.js backend scaffold
- `shared/`: shared protocol/constants/validators
- `data/`: runtime data files

## Quick start

```bash
npm install
npm --prefix client install
npm run dev:server
npm run dev:client
```
